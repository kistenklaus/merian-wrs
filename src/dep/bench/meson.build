# src_files += files('hst.cpp')
# src_files += files('its.cpp')
src_files += files('memcpy.cpp')
src_files += files('partition.cpp')
src_files += files('prefix_sum.cpp')
# src_files += files('psa.cpp')

